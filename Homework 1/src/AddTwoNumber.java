import java.util.List;

public interface AddTwoNumber {

    /**
     * Takes in 2 integer and return an integer list that contains all the numbers in between
     * @param start starting number (exclusive)
     * @param end ending number (exclusive)
     * @return an integer List
     */
    List<Integer> addTwoNumber(int start, int end);
}
